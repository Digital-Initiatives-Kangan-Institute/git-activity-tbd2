## 💡 Case Study
You are a software developer hired by KangaCafe to update their website. KangaCafe has expressed that they do not like the orange color scheme and has asked you to different color schemes to see what fits.

## 📄 Instructions
Using what you have learned about Git, complete the following actions:
1. Fork this repository
2. Clone your copy of the repository
3. Create a new branch
4. Switch to the new branch
5. Open the `index.html` file
6. Change the `--color-scheme-primary` from `orange` to `dodgerblue`
7. Change the `--color-scheme-secondary` from `darkorange` to `skyblue`
8. Stage, commit, and push your changes to GitHub
9. Change the `--color-scheme-primary` from `dodgerblue` to `purple`
10. Change the `--color-scheme-secondary` from `skyblue` to `darkorchid`
11. Stage, commit, and push your changes to GitHub
12. Change the `--color-scheme-primary` from `purple` to `limegreen`
13. Change the `--color-scheme-secondary` from `darkorchid` to `lime`
14. Stage, commit, and push your changes to GitHub
